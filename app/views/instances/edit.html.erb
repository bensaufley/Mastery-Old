<% content_for :title, "Editing instance" %>
<% content_for :subnav do %>
  <%= link_to 'Show', [@instance.activity, @instance] %>
<% end %>

<%= render 'form', locals: { user: @instance.activity.user, activity: @instance.activity, instance: @instance } %>

