<% content_for :title, "Editing activity: #{@activity.name}" %>
<% content_for :subnav do %>
  <%= link_to 'Show', user_activity_path(@activity.user,@activity) %> |
  <%= link_to 'Back', user_path %>
<% end %>

<%= render 'form', user: @user, activity: @activity %>
